
if tasks.length > 0
  +formFor().justify-content-between.mb-3.form-inline
    +select(filterForm, 'taskStatuses', 'taskStatusId', true)
    +select(filterForm, 'executors', 'executorId', true)
    +select(filterForm, 'labels', 'labels.id', true)
    +submit(filterForm).bnt-block
