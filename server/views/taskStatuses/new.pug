extends ../layouts/app.pug

block content
  .row
    .col
    .col-6
      +formFor(action=route('taskStatusesCreate') method='post')
        +input(taskStatusForm, 'name', errors)
        +submit(taskStatusForm).btn-block
    .col